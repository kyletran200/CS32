/*#include "ScoreList.h"
#include <cassert>

int main()
{
	ScoreList s;
	s.add(1);
	s.add(2);
	assert(s.size() == 2);
	assert(s.minimum() == 1);
	assert(s.maximum() == 2);
	s.remove(1);
	assert(s.size() == 1);
}*/